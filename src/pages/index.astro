---
import BaseLayout from '../layouts/BaseLayout.astro';
import { site, services, audiences, recentAdventures, founderNote, images } from '../data/site';
---

<BaseLayout title={site.name}>
  <section class="hero">
    <div class="container hero__grid">
      <div class="hero__content">
        <p class="section-label hero__label">Worldwide travel companions</p>
        <h1>{site.motto}</h1>
        <p class="hero__lead">
          {site.description}
        </p>
        <div class="hero__actions">
          <a href="/contact" class="btn btn--primary">Get a Quote</a>
          <a href="/how-it-works" class="btn btn--secondary">How It Works</a>
        </div>
        <a href={site.phoneHref} class="hero__phone">{site.phone}</a>
      </div>
      <div class="hero__image">
        <img
          src={images.hero.src}
          alt={images.hero.alt}
          width={images.hero.width}
          height={images.hero.height}
          loading="eager"
        />
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <p class="section-label">Who we help</p>
      <h2 class="section-title">Travel made possible for everyone</h2>
      <p class="section-intro">
        Whether you are planning for yourself or a loved one, we provide the support that turns hesitation into confidence.
      </p>
      <div class="grid-2 audience-grid">
        {
          audiences.map((item) => (
            <article class="audience-card">
              <div class="audience-card__image">
                <img src={item.image} alt={item.imageAlt} width={600} height={400} loading="lazy" />
              </div>
              <div class="audience-card__body">
                <h3>{item.title}</h3>
                <p>{item.description}</p>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section section--alt">
    <div class="container">
      <p class="section-label">Our services</p>
      <h2 class="section-title">Everything you need, door to door</h2>
      <div class="services-grid">
        {
          services.slice(0, 6).map((service) => (
            <article class="card">
              <h3>{service.title}</h3>
              <p>{service.description}</p>
            </article>
          ))
        }
      </div>
      <div class="section-cta">
        <a href="/services" class="btn btn--primary">View All Services</a>
      </div>
    </div>
  </section>

  <section class="section cta-banner">
    <div class="container cta-banner__inner">
      <div>
        <h2>Ready to plan your trip?</h2>
        <p>Contact us for a personalized quote. No obligation — just a conversation about how we can help.</p>
      </div>
      <div class="cta-banner__actions">
        <a href="/contact" class="btn btn--light">Get a Quote</a>
        <a href={site.phoneHref} class="btn btn--secondary cta-banner__phone">{site.phone}</a>
      </div>
    </div>
  </section>

  <section class="section editorial">
    <div class="container editorial__grid">
      <div class="editorial__image">
        <img
          src={images.mission.src}
          alt={images.mission.alt}
          width={images.mission.width}
          height={images.mission.height}
          loading="lazy"
        />
      </div>
      <div class="editorial__content">
        <p class="section-label">Our mission</p>
        <h2>Companion support for every mile</h2>
        <p>
          Don&rsquo;t let getting there keep you from going. We have expanded our services to include staying with you throughout your adventure — providing assistance at cruise ports, historic sites, theme parks, and every moment in between.
        </p>
        <p>
          While you are on your vacation, we are working. A lot of people need just a little help to achieve their travel dreams — and that is exactly what we are here for.
        </p>
        <p>
          <strong>{site.founder.name}, {site.founder.credentials}</strong><br />
          {site.founder.title}, {site.name}
        </p>
        <a href="/about" class="btn btn--secondary">Our Story</a>
      </div>
    </div>
  </section>

  <section class="section section--alt">
    <div class="container">
      <p class="section-label">Recent adventures</p>
      <h2 class="section-title">Lots of TH travels lately!</h2>
      <p class="section-intro">
        From Caribbean cruises to Las Vegas and the Greek isles — here is a glimpse of where Travel Helpers has been making adventures possible.
      </p>
      <div class="grid-3 adventures-grid">
        {
          recentAdventures.map((trip) => (
            <article class="adventure-card card">
              <div class="adventure-card__image">
                <img src={trip.image} alt={trip.imageAlt} width={600} height={400} loading="lazy" />
              </div>
              <h3>{trip.title}</h3>
              <p>{trip.description}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section editorial editorial--note">
    <div class="container editorial__grid">
      <div class="editorial__content">
        <p class="section-label">Travel wisely</p>
        <h2>{founderNote.title}</h2>
        <p>{founderNote.intro}</p>
        <ul class="founder-note__list">
          {founderNote.points.map((point) => <li>{point}</li>)}
        </ul>
        <p><strong>{founderNote.closing}</strong></p>
        <p>
          <strong>{site.founder.name}, {site.founder.credentials}</strong><br />
          {site.founder.title}, {site.name}
        </p>
        <a href={site.phoneHref} class="btn btn--primary">Call {site.phone}</a>
      </div>
      <div class="founder-note__gallery">
        {
          founderNote.gallery.map((photo) => (
            <img src={photo.src} alt={photo.alt} width={400} height={300} loading="lazy" />
          ))
        }
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding: 3rem 0 0;
    overflow: hidden;
  }

  .hero__grid {
    display: grid;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 960px) {
    .hero__grid {
      grid-template-columns: 1fr 1fr;
      min-height: calc(100vh - var(--header-height));
    }
  }

  .hero__label {
    font-size: clamp(1rem, 1.75vw, 1.25rem);
    letter-spacing: 0.12em;
    margin-bottom: 1.25rem;
  }

  .hero__content h1 {
    font-size: clamp(2.75rem, 5.5vw, 4.5rem);
    margin-bottom: 1.5rem;
  }

  .hero__lead {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    max-width: 34rem;
    margin-bottom: 2rem;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .hero__phone {
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--color-accent);
  }

  .hero__image img {
    width: 100%;
    height: 100%;
    min-height: 24rem;
    object-fit: cover;
    border-radius: var(--radius);
  }

  .audience-grid {
    margin-top: 3rem;
  }

  .audience-card {
    display: grid;
    gap: 1.5rem;
    padding: 2rem 0;
    border-top: 1px solid var(--color-border);
  }

  @media (min-width: 640px) {
    .audience-card {
      grid-template-columns: 11rem 1fr;
      align-items: center;
    }
  }

  .audience-card__image {
    border-radius: var(--radius);
    overflow: hidden;
    aspect-ratio: 3 / 2;
    box-shadow: var(--shadow);
  }

  .audience-card__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .audience-card h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .audience-card p {
    color: var(--color-text-muted);
  }

  .services-grid {
    display: grid;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  @media (min-width: 768px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .section-cta {
    margin-top: 2.5rem;
    text-align: center;
  }

  .editorial__grid {
    display: grid;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .editorial__grid {
      grid-template-columns: 1.1fr 1fr;
    }
  }

  .editorial__image img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: var(--radius);
  }

  .editorial__content h2 {
    font-size: clamp(2rem, 3.5vw, 2.75rem);
    margin-bottom: 1.25rem;
  }

  .editorial__content p {
    color: var(--color-text-muted);
    margin-bottom: 1rem;
  }

  .editorial__content .btn {
    margin-top: 1.5rem;
  }

  .adventures-grid {
    margin-top: 3rem;
  }

  .adventure-card {
    padding: 0;
    overflow: hidden;
  }

  .adventure-card__image {
    aspect-ratio: 3 / 2;
    overflow: hidden;
  }

  .adventure-card__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .adventure-card h3 {
    font-size: 1.25rem;
    padding: 1.25rem 1.25rem 0.5rem;
  }

  .adventure-card p {
    font-size: 0.9375rem;
    color: var(--color-text-muted);
    padding: 0 1.25rem 1.25rem;
    margin: 0;
  }

  .editorial--note .editorial__grid {
    align-items: start;
  }

  .founder-note__list {
    margin: 1.25rem 0;
    padding-left: 1.25rem;
    color: var(--color-text-muted);
  }

  .founder-note__list li {
    margin-bottom: 0.75rem;
  }

  .founder-note__gallery {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
  }

  .founder-note__gallery img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: var(--radius);
  }

  .cta-banner {
    background: var(--color-accent);
    color: #fff;
  }

  .cta-banner__inner {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    align-items: flex-start;
  }

  @media (min-width: 768px) {
    .cta-banner__inner {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  .cta-banner h2 {
    font-size: clamp(2rem, 3vw, 2.75rem);
    margin-bottom: 0.75rem;
  }

  .cta-banner p {
    opacity: 0.9;
    max-width: 32rem;
  }

  .cta-banner__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .cta-banner__phone {
    border-color: rgba(255, 255, 255, 0.4);
    color: #fff;
  }

  .cta-banner__phone:hover {
    border-color: #fff;
    background: rgba(255, 255, 255, 0.1);
  }
</style>
